package constants

const (
	PING_COMMAND = "PING"
	ECHO_COMMAND = "ECHO"
	GET_COMMAND  = "GET"
	SET_COMMAND  = "SET"
)

const (
	PONG_RESPONSE = "PONG"
)

const (
	SUB_COMMAND_FORMAT = "%s_%s"
)

// Sub-commands
const (
	SET_PX_COMMAND = "SET_PX"
)
